<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.achobeta.infrastructure.dao.PositionMapper">

    <resultMap id="PositionResultMap" type="com.achobeta.infrastructure.dao.po.PositionPO">
        <id property="id" column="id"/>
        <result property="teamId" column="team_id"/>
        <result property="positionName" column="position_name"/>
        <result property="positionId" column="position_id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="addPositionToUser">
        insert into user_position (user_id, position_id, create_by) values
        <foreach collection="positionList" item="positionId" separator=",">
        (#{userId}, #{positionId}, #{userId})
        </foreach>
    </insert>

    <update id="deletePositionWithUser">
        update user_position set is_deleted = 1
        where user_id = #{userId} and position_id in
        <foreach collection="deletePositions" item="positionId" separator="," open="(" close=")">
        #{positionId}
        </foreach>
    </update>

</mapper>